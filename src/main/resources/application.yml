server:
    port: 5000

spring:
    datasource:
        url: jdbc:postgresql://localhost:5332/city
        username: mart624i
        password: pw123
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show-sql: true
    main:
      web-application-type: servlet